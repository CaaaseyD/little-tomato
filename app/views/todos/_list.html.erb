<% todos.each do |todo| %>
  <%= turbo_frame_tag dom_id(todo) do %>
    <div class="bg-white p-6 rounded-lg shadow flex items-center mb-4">
        <% if todo.done? %>
            <%= link_to todo_path(todo, todo: { done: false }), class: 'shrink-0 mr-4', data: { turbo_method: :patch } do %>
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6">
                  <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12zm13.36-1.814a.75.75 0 10-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 00-1.06 1.06l2.25 2.25a.75.75 0 001.14-.094l3.75-5.25z" clip-rule="evenodd" />
                </svg>
            <% end %>
        <% else %>
            <%= link_to todo_path(todo, todo: { done: true }), class: 'shrink-0 mr-4', data: { turbo_method: :patch } do %>
                <div class="h-6 w-6 rounded-full bg-primary-200"></div>
            <% end %>
        <% end %>
      <span class="text-gray-900"><%= todo.title %></span>
    </div>
  <% end %>
<% end %>
